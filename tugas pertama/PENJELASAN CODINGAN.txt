PENJELASAN CODINGAN TUGAS 1

import 'package:flutter/material.dart'; //bagian import

void main() { //bagian main
  runApp(new MaterialApp( // widget yang membungkus beberapa widget yang menggunakan tema material design
    title: "Tugas Mobile 1", //judul aplikasi untuk recent app
    home: new HalamanSatu(),
    debugShowCheckedModeBanner: false, //agar tulisan debug hilang
  ));
}

class HalamanSatu extends StatelessWidget { //class widget yang propertinya immutable atau tidak bisa diubah
  @override
  Widget build(BuildContext context) {
    return new Scaffold( //widget untuk struktur dasar material design
      backgroundColor: Colors.white, //warna background
      appBar: new AppBar( //widget untuk membuat AppBar
        backgroundColor: Colors.red[700], //warna appbar
        leading: new Icon(Icons.home), //icon home di leading appbar
        title: new Center(child: new Text("Kelompok 1 dan SDGs 7"),), //judul appbar
        actions: <Widget>[
          new Icon(Icons.search) //icon search di appbar
        ],
      ),
      body: Column( //widget layout untuk body yang dapat berisi beberapa layout.
        crossAxisAlignment: CrossAxisAlignment.start, //agar rata kiri
        children: <Widget>[
          Container(
            padding: EdgeInsets.fromLTRB(20, 40, 20, 20), //untuk spasi antara konten dan border
            child: Text("Anggota Kelompok :", style: TextStyle(fontSize: 16))), //teks untuk ditampilkan pada body

          Padding(
            padding: EdgeInsets.fromLTRB(20, 10, 20, 10), //untuk spasi antara konten dan border
            child: Text("1. Febria Erliana Hadi - 182410102003", style: TextStyle(fontSize: 16))), //teks untuk ditampilkan pada body

          Padding(
            padding: EdgeInsets.fromLTRB(20, 10, 20, 10), //untuk spasi antara konten dan border
            child: Text("2. Ashoh Bagus Jundan  - 182410102034", style: TextStyle(fontSize: 16))), //teks untuk ditampilkan pada body

          Padding(
            padding: EdgeInsets.fromLTRB(20, 10, 20, 10), //untuk spasi antara konten dan border
            child: Text("3. Miantoko Gundho P N - 182410102049", style: TextStyle(fontSize: 16))), //teks untuk ditampilkan pada body

          Padding(
            padding: EdgeInsets.fromLTRB(20, 10, 20, 10), //untuk spasi antara konten dan border
            child: Text("4. Sylvia Rizky Anissa - 182410102052", style: TextStyle(fontSize: 16))), //teks untuk ditampilkan pada body

        ],
      )
    );
  }
}